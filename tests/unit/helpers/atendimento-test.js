import { atendimento } from 'julia/helpers/atendimento';
import { module, test } from 'qunit';

module('Unit | Helper | atendimento');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = atendimento([42]);
  assert.ok(result);
});
